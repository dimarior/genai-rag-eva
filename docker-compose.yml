services:
  prometheus:
    image: prom/prometheus:latest
    container_name: rag-eva-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./docker/prometheus.yml:/etc/prometheus/prometheus.yml

  grafana:
    image: grafana/grafana:latest
    container_name: rag-eva-grafana
    ports:
      - "3000:3000"
    depends_on:
      - prometheus
